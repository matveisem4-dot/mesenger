name: Ultimate-Fix-APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '11'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 7.6
          cache-disabled: true

      # ДОБАВЛЯЕМ НЕДОСТАЮЩИЕ ИНСТРУМЕНТЫ
      - name: Install Android Build Tools
        run: |
          echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --sdk_root=$ANDROID_HOME "build-tools;32.0.0"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Build Real APK
        run: |
          npm install -g cordova
          cordova create ghost-app com.ghost.net GhostNet
          cd ghost-app
          cordova platform add android@11.0.0 --no-telemetry
          cp ../index.html www/index.html
          # Собираем APK
          cordova build android --debug --no-telemetry

      - name: Upload APK File
        uses: actions/upload-artifact@v4
        with:
          name: GhostNet-Official-APK
          path: ghost-app/platforms/android/app/build/outputs/apk/debug/app-debug.apk
